<!-- <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Image Viewer with WebSocket</title>
  </head>
  <body>
    <h1>Cam</h1>
    <img src="/uploads/img.jpg" alt="Uploaded Image" id="img_class" />

    <script>
      const fetch = () => {
        console.log("fetch!");
        const img = document.querySelector("#img_class");
        const timestamp = new Date().getTime(); // Generate a unique timestamp
        img.src = `/uploads/img.jpg?t=${timestamp}`;
      };
      setInterval(() => {
        fetch();
      }, 1000);
    </script>
  </body>
</html> -->
